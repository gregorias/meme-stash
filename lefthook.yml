pre-commit:
  commands:
    prettier:
      run: "prettier --check ."
    unit-tests:
      run: "npm run test"
    build:
      run: "npm run build"
commit-msg:
  commands:
    commitlint:
      run: "commitlint --edit={1}"
